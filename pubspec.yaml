name: flutter_6_9
description: "A new Flutter project."

version: 0.1.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:

  cloud_firestore: ^5.4.1
  firebase_core: ^3.4.1
  flutter:
    sdk: flutter
  provider: ^6.1.2
  firebase_auth: ^5.3.0  # หรือเวอร์ชันล่าสุด
  intl: ^0.17.0
  fl_chart: ^0.69.0
  syncfusion_flutter_charts: ^20.2.40

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
